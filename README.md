# راهنما قدم‌به‌قدم نصب نرم‌افزار ویندوزی (تحلیل‌گران) روی لینوکس
# Tahlilgaran Desktop (Windows Version on Linux)

نسخه اندروید دیکشنری تحلیل‌گران رایگان است، اما نسخه دسکتاپ ویندوزی پولی بوده و نیاز به خرید لایسنس دارد.

با این نرم‌افزار می‌توانید به دیکشنری‌های معتبری مانند **Longman**، **Oxford** و سایر منابع به‌صورت هم‌زمان دسترسی داشته باشید.

🔗 لینک خرید نسخه ویندوز:
[https://www.tahlilgaran.org/Course/eAppStore/?emedia=winapp&clx=10000](https://www.tahlilgaran.org/Course/eAppStore/?emedia=winapp&clx=10000)

---

# راهنمای نصب نسخه ویندوزی تحلیل‌گران روی لینوکس با Bottles

این راهنما روی **Ubuntu Desktop** تست شده است، اما روی سایر توزیع‌های مبتنی بر Flatpak نیز قابل اجراست.

---

## پیش‌نیازها

### 1) نصب Flatpak و فعال‌سازی Flathub

اگر Flatpak روی سیستم شما نصب نیست:

### Ubuntu / Debian

```bash
sudo apt install flatpak
```

### Fedora

```bash
sudo dnf install flatpak
```

### Arch Linux

```bash
sudo pacman -S flatpak
```

سپس مخزن Flathub را اضافه کنید:

```bash
sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
```

سیستم را یک‌بار ری‌استارت کنید.

---

## 2) نصب Bottles

Bottles محیطی برای اجرای برنامه‌های ویندوزی از طریق Wine است.

نصب از Flathub:

```bash
flatpak install flathub com.usebottles.bottles
```

اجرای Bottles:

```bash
flatpak run com.usebottles.bottles
```

> نیازی به نصب جداگانه Wine یا Winetricks نیست؛ Bottles وابستگی‌های لازم را مدیریت می‌کند.

---

# مراحل نصب نرم‌افزار تحلیل‌گران

## 1) ساخت یک Bottle جدید

1. Bottles را باز کنید.
2. روی **Create a New Bottle** کلیک کنید.
3. نوع Bottle را روی **Custom** قرار دهید.
4. Runner را روی **Soda** تنظیم کنید.
5. یک نام مناسب انتخاب کنید (مثلاً: `TDictionary`).
6. منتظر بمانید تا ساخت Bottle کامل شود.

---

## 2) نصب پیش‌نیاز داخل Bottle

1. وارد Bottle ساخته‌شده شوید.

2. به مسیر زیر بروید:

   ```
   Options → Dependencies
   ```

3. گزینه زیر را نصب کنید:

   ```
   dotnet48
   ```

این مرحله ضروری است؛ نرم‌افزار تحلیل‌گران بدون .NET Framework 4.8 اجرا نخواهد شد.

---

## 3) نصب نرم‌افزار تحلیل‌گران

1. داخل Bottle روی **Run Executable** کلیک کنید.
2. فایل نصب نرم‌افزار (`Setup.exe`) را انتخاب کنید.
3. مراحل نصب را مانند ویندوز تا پایان انجام دهید.

---

## 4) نصب Wine Gecko (در اجرای اول)

در اولین اجرای برنامه ممکن است پیام زیر نمایش داده شود:

```
Wine could not find a Gecko package...
```

این پیام طبیعی است و نشان می‌دهد مؤلفه **Wine Gecko** داخل Bottle نصب نشده است.

مراحل انجام کار:

1. روی **Install** کلیک کنید.
2. منتظر بمانید تا دانلود و نصب به‌صورت خودکار انجام شود.
3. پس از نصب، برنامه بدون مشکل اجرا خواهد شد.

این فرآیند فقط یک‌بار انجام می‌شود و نیازی به نصب دستی Wine یا Gecko به‌صورت جداگانه نیست.

---

## 5) افزودن برنامه به Library

پس از اتمام نصب:

1. وارد Bottle شوید.
2. روی سه‌نقطه برنامه کلیک کنید.
3. گزینه **Add to Library** را انتخاب کنید.

اکنون می‌توانید برنامه را مستقیماً از Library اجرا کنید.

---

## تنظیمات پیشنهادی (اختیاری اما توصیه‌شده)

در تنظیمات Bottles:

```
Preferences → Experiments
```

گزینه زیر را فعال کنید:

```
Sandbox per bottle
```

این تنظیم باعث ایزوله شدن هر Bottle و جلوگیری از تداخل احتمالی می‌شود.

---

# پیکربندی پیشنهادی نهایی

* نوع Bottle: Custom
* Runner: Soda
* Dependency نصب‌شده: dotnet48
* Wine Gecko: نصب در اولین اجرا با گزینه Install (که ممکن است نیاز به VPN داشته باشید)
* Sandbox per bottle: فعال

این ترکیب روی Ubuntu Desktop بدون مشکل اجرا شده است.
